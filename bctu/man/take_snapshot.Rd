% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot.R
\name{take_snapshot}
\alias{take_snapshot}
\title{Fetch a source and save it as an immutable snapshot}
\usage{
take_snapshot(
  source,
  store = snapshot_store(create = TRUE),
  formats = c("rds", "csv"),
  tag = NULL,
  labels = NULL,
  git = NULL,
  verbose = 2L
)
}
\arguments{
\item{source}{A \link[readr:datasource]{datasource}.}

\item{store}{Snapshot store directory; default resolved from the project marker.}

\item{formats}{Payload formats to write; default \code{c("rds","csv")}. Other
values (\code{"dta"}, \code{"sas7bdat"}, \code{"xpt"}, \code{"sas"}) are written by
\code{\link[=save_snapshot]{save_snapshot()}}.}

\item{tag}{Optional short tag for this extraction (e.g. \code{"DMC-2026-08"}),
recorded on the snapshot, its tables, the manifest and the audit ledger, and
used to annotate the git tag.}

\item{labels}{Optional named list of extra free-text metadata to record.}

\item{git}{Git provenance mode; see \code{\link[=save_snapshot]{save_snapshot()}}.}

\item{verbose}{Verbosity.}
}
\value{
The saved snapshot, with its \code{id} attached, invisibly.
}
\description{
Fetch a source and save it as an immutable snapshot
}
\examples{
\dontrun{
ds <- datasource_example()
take_snapshot(ds)
}
}
