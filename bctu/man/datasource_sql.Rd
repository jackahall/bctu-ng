% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{datasource_sql}
\alias{datasource_sql}
\title{A SQL Server datasource (multiple tables/views)}
\usage{
datasource_sql(
  server,
  database,
  tables = NULL,
  views_query = NULL,
  conn = sql_connection(),
  include = NULL,
  connector = NULL,
  connect_args = NULL,
  name = NULL,
  ...
)
}
\arguments{
\item{server}{SQL Server host (or instance).}

\item{database}{Database name.}

\item{tables}{Optional character vector of table/view names to load.}

\item{views_query}{Optional discovery query returning object names in its
first column. Used when \code{tables} is \code{NULL}.}

\item{conn}{An \code{\link[=sql_connection]{sql_connection()}} describing the ODBC connection.}

\item{include}{Optional regular expression; when discovering via
\code{views_query}, keep only names matching it (a view-subset toggle).}

\item{connector}{Optional DBI driver object to inject (default \code{odbc::odbc()}
at fetch time). Used for testing against other backends (e.g.
\code{RSQLite::SQLite()}).}

\item{connect_args}{Optional named list of \code{DBI::dbConnect()} arguments to
use verbatim with \code{connector}, bypassing the ODBC argument builder.}

\item{name}{Optional snapshot/label name; defaults to \code{database}.}

\item{...}{Reserved for future use.}
}
\value{
A \code{datasource} object.
}
\description{
Connects to a SQL Server database and loads a set of tables or views into a
named list of data frames (a multi-table snapshot). The table set is either
given explicitly (\code{tables}) or discovered by running \code{views_query} (a query
returning object names in its first column, e.g.
\code{"SELECT name FROM sys.views WHERE name LIKE 'vw\%'"}); each discovered object
is then loaded with \verb{SELECT * FROM <name>} (the name quoted as an identifier).
}
\details{
The full ODBC connection surface is honoured via \code{\link[=sql_connection]{sql_connection()}}. To test
or use a non-SQL-Server backend, inject a different DBI \code{connector} (the
driver object) and \code{connect_args}; by default the connector is
\code{odbc::odbc()} and the connection arguments are built from \code{server},
\code{database} and \code{conn}.

Datetimes are returned exactly as the database driver yields them; bctu does
not silently coerce timezones (a known GMT/BST trap). Apply an explicit
timezone in your analysis if the DB column is timezone-naive.

If any requested object loads as something other than a data frame (a view
whose SQL errored can arrive as a character error message), the fetch aborts
loudly naming the offending object rather than snapshotting the error.
}
\seealso{
\code{\link[=sql_connection]{sql_connection()}}, \code{\link[=datasource_redcap]{datasource_redcap()}}, \code{\link[=take_snapshot]{take_snapshot()}}
}
