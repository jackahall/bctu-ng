% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{check_setup}
\alias{check_setup}
\title{Check this machine is correctly set up to use bctu (installation IQ/OQ)}
\usage{
check_setup(
  store = NULL,
  formats = c("docx", "pdf"),
  require_credentials = NULL,
  verbose = 2L
)
}
\arguments{
\item{store}{Optional snapshot store directory to test writing to. If \code{NULL}
(the default), the store is resolved from the current bctu project; if no
project marker is found the store check is reported as an informational skip
with the fix, not a failure.}

\item{formats}{Report output formats you intend to produce; the rendering
toolchain is checked only for these. \code{"pdf"} additionally requires a LaTeX
engine; a \code{"docx"}-only user is never failed for missing LaTeX. Default
\code{c("docx", "pdf")}.}

\item{require_credentials}{Optional \code{\link[=credential_spec]{credential_spec()}} (or list of them) to
confirm are available on this machine. Reported as PRESENT or MISSING only;
the secret value is never resolved into output, printed, or returned.}

\item{verbose}{\code{2} prints the full table and the overall verdict (default),
\code{1} prints only the overall verdict, \code{0} prints nothing.}
}
\value{
Invisibly, a \code{bctu_setup_qualification} list with \code{ok} (overall pass),
\code{checks} (a data frame of every check), \code{environment} (the captured
environment), \code{formats}, and \code{time}.
}
\description{
Runs a battery of named installation-qualification / operational-qualification
checks, prints an IQ/OQ-style table (Check / Result / Detail) with an overall
PASS or FAIL, and returns (invisibly) a structured result you can archive with
\code{\link[=write_setup_report]{write_setup_report()}}. This is validation layer (a): it qualifies that R, the
system toolchain and the bctu dependencies are installed correctly and operate
to spec on this machine. It is modelled on the R Foundation regulatory document
R-FDA.pdf and the marcschwartz/R-IQ-OQ approach.
}
\details{
Formal validation of the bctu package itself is done separately with the
pharmaR \code{valtools} package at release time (requirements, test cases,
traceability matrix, validation report), and dependency risk is scored with
pharmaR \code{riskmetric} via \code{\link[=package_risk_report]{package_risk_report()}}. bctu does not recreate
those frameworks; \code{check_setup()} is only the installation IQ/OQ.
}
