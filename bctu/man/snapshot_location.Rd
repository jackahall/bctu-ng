% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{snapshot_location}
\alias{snapshot_location}
\title{Resolve where snapshots are saved, falling back to the working directory}
\usage{
snapshot_location(start = getwd(), verbose = 1L)
}
\arguments{
\item{start}{Directory to search upward from for a project marker; default
the current directory.}

\item{verbose}{If \verb{>= 1}, announce the resolved location and its source.}
}
\value{
The absolute directory where snapshots are read and written.
}
\description{
Unlike \code{\link[=snapshot_store]{snapshot_store()}}, which requires a \code{bctu-project.yml} marker and
errors when none is found, this resolver falls back to the working directory
so snapshots can be taken outside a project. The chosen location is ALWAYS
announced, so it is never a silent guess.
}
\details{
If a project marker is found by walking up from \code{start}, the project's
snapshot store is used and announced with its source marker. Otherwise the
current working directory is used and announced, with a hint to run
\code{\link[=bctu_init_project]{bctu_init_project()}} or pass \verb{store=} to change it.
}
