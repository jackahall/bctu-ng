% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sources.R
\name{datasource_redcap}
\alias{datasource_redcap}
\title{A REDCap datasource (one table: the exported records)}
\usage{
datasource_redcap(
  token_id,
  url,
  report_id = NULL,
  labelled = TRUE,
  missing_codes = NULL,
  service = "bctu_api_token",
  name = NULL,
  ...
)
}
\arguments{
\item{token_id}{Logical credential name for the REDCap API token (resolved
via \code{\link[=credential_spec]{credential_spec()}} / \code{\link[=resolve_credentials]{resolve_credentials()}}). REDCap tokens are 32
characters.}

\item{url}{REDCap API endpoint, e.g. \code{"https://redcap.example.org/api/"}.}

\item{report_id}{Optional saved-report id. If given, exports that report
instead of all records.}

\item{labelled}{If \code{TRUE} (default), apply REDCap value labels (from the data
dictionary) to coded fields using haven-style labelled vectors. Set \code{FALSE}
to keep raw codes.}

\item{missing_codes}{Optional \code{\link[=special_missing]{special_missing()}} mapping. When given, the
named missing-data codes (e.g. \code{"UNK"}, \code{"OTH"}) are converted to native
special missing values at extraction time and each column keeps its natural
type, instead of the whole column coercing to character.}

\item{service}{Keyring service for the token; default \code{"bctu_api_token"}. Set
a per-project service to avoid sharing one keyring entry across trials.}

\item{name}{Optional snapshot/label name; defaults to \code{"redcap"}.}

\item{...}{Extra REDCap API parameters passed on the record/report export
(e.g. \code{rawOrLabel}, \code{exportDataAccessGroups}, \code{filterLogic}).}
}
\value{
A \code{datasource} object.
}
\description{
Exports records from a REDCap project over its API and returns them as a
single-table snapshot. When \code{report_id} is given, a saved REDCap report is
exported instead of the full record set. The project data dictionary and the
export field names are fetched alongside the records and attached to the
records data frame as attributes (\code{redcap_dictionary}, \code{redcap_field_names})
so downstream steps can label or reshape the data.
}
\details{
The API token is never passed here. Declare where it lives with \code{token_id}
(a \code{\link[=credential_spec]{credential_spec()}} keyed on that id, expecting the 32-character REDCap
token) and the resolver reads it from the keyring or environment at fetch
time.

Datetimes are returned exactly as REDCap sends them (text); no timezone is
guessed or applied.
}
\seealso{
\code{\link[=datasource_sql]{datasource_sql()}}, \code{\link[=special_missing]{special_missing()}}, \code{\link[=take_snapshot]{take_snapshot()}}
}
