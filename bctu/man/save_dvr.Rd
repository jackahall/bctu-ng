% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{save_dvr}
\alias{save_dvr}
\title{Build a Data Validation Report (DVR) from a snapshot and a check}
\usage{
save_dvr(
  snapshot,
  check,
  output_dir,
  operator = NULL,
  write_xlsx = TRUE,
  per_site = FALSE,
  site_field = "site",
  verbose = 2L
)
}
\arguments{
\item{snapshot}{A saved \code{bctu_snapshot} (must carry \code{id} and \code{dir}).}

\item{check}{A \code{\link[=bctu_check]{bctu_check()}}.}

\item{output_dir}{Directory to hold DVRs and the DVR ledger.}

\item{operator}{Person issuing the report (recorded); default the OS user.}

\item{write_xlsx}{Also write an Excel workbook if \code{openxlsx} is available?}

\item{per_site}{Write per-site sheets in the workbook?}

\item{site_field}{Name of the site column in the data. Default \code{"site"}.}

\item{verbose}{Verbosity.}
}
\value{
Invisibly, a list with the report id, paths, and per-class counts.
}
\description{
Runs \code{check} on \code{snapshot}, writes the FULL findings set and an UPDATE set
(new / changed / resolved since the last issued DVR), records an auditable
manifest and a DVR-ledger entry, and returns the result invisibly. The
update diff is computed against the snapshot the LAST ISSUED DVR was built
from (read from the DVR ledger), never against a directory position.
}
