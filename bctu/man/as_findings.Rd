% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{as_findings}
\alias{as_findings}
\title{Validate and normalise a findings table at the check boundary}
\usage{
as_findings(x)
}
\arguments{
\item{x}{A data frame, a findings table, or \code{NULL}/zero rows (treated clean).}
}
\value{
A \code{bctu_findings} data frame.
}
\description{
Accepts a data frame produced by a check and returns it in the fixed
schema. Rejects any column that is not part of the schema (so a check cannot
quietly introduce a column that would break the update diff), and requires
the mandatory columns. Missing optional columns are added as \code{NA}. All
values are stored as text so findings compare cleanly across reports.
}
