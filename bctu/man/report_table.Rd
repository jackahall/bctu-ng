% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table.R
\name{report_table}
\alias{report_table}
\title{Build a format-agnostic report table}
\usage{
report_table(
  data,
  columns = NULL,
  caption = NULL,
  group_headers = NULL,
  banner_rows = NULL,
  align = NULL
)
}
\arguments{
\item{data}{A data frame: the body of the table (one row per table row).}

\item{columns}{Which columns to show and their headings. One of:
\itemize{
\item \code{NULL} (default) — show every column of \code{data}, heading = column name;
\item a character vector of column names — show those columns, in that order,
heading = column name;
\item a \emph{named} character vector — names are columns of \code{data}, values are the
headings shown in the table.
}}

\item{caption}{Optional table caption (a single string).}

\item{group_headers}{Optional spanning headers shown above the column
headings. Either a data frame with columns \code{label} and \code{span}, or a named
integer vector (names = labels, values = number of columns spanned). The
spans must add up to the number of displayed columns, left to right.}

\item{banner_rows}{Optional full-width rows inserted into the body (for
example a group separator). A list of \code{list(label = , after = )} entries,
or a data frame with columns \code{label} and \code{after}, where \code{after} is the body
row number the banner follows (\code{0} = before the first row).}

\item{align}{Optional column alignment: a character vector, one of
\code{"left"}, \code{"right"}, \code{"center"} per displayed column. If \code{NULL} (default),
numeric columns are right-aligned and all others left-aligned.}
}
\value{
A \code{bctu_report_table} object.
}
\description{
Returns a single structured object that both the Word (grid table) and PDF
(LaTeX) renderers consume, so a table is described once and rendered the same
way everywhere. The body data keeps its original types, so the exact numbers
behind the report are always retrievable with \code{\link[=report_table_data]{report_table_data()}}.
}
\examples{
data <- data.frame(id = 1:3, age = c(45, 62, 38), sex = c("F", "M", "F"))
report_table(data, caption = "Baseline characteristics")
}
