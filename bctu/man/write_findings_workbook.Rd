% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{write_findings_workbook}
\alias{write_findings_workbook}
\title{Write a set of checks to one Excel workbook, one worksheet per check}
\usage{
write_findings_workbook(sheets, path)
}
\arguments{
\item{sheets}{A named list of findings data frames.}

\item{path}{Workbook path (\code{.xlsx}).}
}
\value{
Invisibly, the path (or \code{NULL} if \code{openxlsx} is unavailable or there
is nothing to write).
}
\description{
A convenience only: the CSV/TXT copies carry the same content, so the DVP
logic is fully testable without Excel. Uses \code{openxlsx} if installed; if not,
warns and does nothing. Worksheet names are the check names, trimmed to
Excel's 31-character limit (de-duplicated if trimming collides).
}
