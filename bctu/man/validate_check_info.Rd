% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{validate_check_info}
\alias{validate_check_info}
\title{Validate a check-info table (per-check query text and labels)}
\usage{
validate_check_info(info, check_names)
}
\arguments{
\item{info}{A data frame as described above.}

\item{check_names}{The check names the DVP produced this run.}
}
\value{
\code{info}, with columns ordered \code{check}, \code{section}, \code{critical}, \code{query}
(those present), row order preserved.
}
\description{
A check-info table describes the checks in DM-facing language: one row per
check with the query text a data manager acts on, plus optional grouping and
criticality labels. Required columns: \code{check} (unique, matching the names the
DVP function returns) and \code{query} (non-empty text). Optional columns:
\code{section} (free text) and \code{critical} (logical), settable independently.
Checks with findings but no info row, and info rows matching no check, are
warned about (not errors), so a partially annotated DVP still reports.
}
\keyword{internal}
