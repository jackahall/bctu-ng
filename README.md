# bctu-ng — bctu package rebuild (workspace)

Superfolder workspace for the ground-up rebuild of the bctu R package.
- `bctu/` — the package itself (the deliverable).
- The existing package at `~/projects/bctu/bctu` is the reference and is left untouched.

Rebuild spec: `~/projects/bctu/BCTU_REVIEW_AND_REBUILD_PLAN.md`.
